daemon off;
master_process off;

env TEST_MARKER;

error_log logs/error.log notice;
pid logs/nginx.pid;

events {}

http {
    access_log off;

    server {
        listen 127.0.0.1:18080;

        location /hello {
            lua_content app/hello.lua;
        }
    }
}
